{
  "name": "explore_gallery_api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "start": "json-server --watch db.json --port $PORT --no-static"
},

  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
